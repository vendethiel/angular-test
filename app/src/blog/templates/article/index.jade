extends ../layout

block body
	div#articles(ng-controller="ArticleCtrl")
		article(ng-repeat="article in articles")
			.infos By {{article.author}} in {{article.category}}
			p {{article.content}}
